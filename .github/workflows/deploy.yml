name: Auto Build & Deploy to deploy_branch

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout source code
      uses: actions/checkout@v3

    - name: Setup Node
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install

    - name: Build project
      run: npm run build

    - name: Deploy dist/ to deploy branch
      run: |
        cd dist
        git init
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"
        git remote add origin https://x-access-token:${{ secrets.GH_PAT }}@github.com/dataseatech/dataseatech.git
        git add .
        git commit -m "Auto-deploy"
        git branch -M deploy
        git push -f origin deploy

